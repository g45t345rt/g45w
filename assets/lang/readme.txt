We don't need a dictionary for English e.g en.json. 
The program is using hardcoded english text values as keys and display on screen if there is no translation.

Use `py make_lang_dict.py` to regenerate the template.