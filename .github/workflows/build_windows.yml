name: Build Windows

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-leatest

    steps:
    - name: Checkout repo
      uses: actions/checkout@v2

    - name: Setup Go
      uses: actions/setup-go@v2
      with:
        go-version: 1.19.10

    - name: Install gogio tool
      run: go install gioui.org/cmd/gogio@latest

    - name: Build windows amd64
      run: ./build_gio.sh windows amd64

    - name: Archive build
      uses: actions/upload-artifact@v2
      with:
        name: Build
        path: build